# Environments
.venv/
venv/
__pycache__/
*.py[cod]

# Secrets. Credentials live in ~/.pems_env, never in the repo.
.env
.env.*
!.env.example
*.pem

# Collected data lives outside the repo, in ~/traffic-data.
data/
*.txt.gz
*.json.gz

# Large derived artifacts
*.parquet

# Boosters are build output, rebuilt by `make train`. The metrics beside them
# are committed, since they record what the model scored.
models/**/model.txt
models/model.txt

# OS / editor
.DS_Store
.idea/
.vscode/
