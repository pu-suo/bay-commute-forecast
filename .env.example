# Copy to ~/.pems_env (NOT into this repo) and chmod 600.
# Register at https://pems.dot.ca.gov/?dnode=apply — approval takes 1-2 business days.
export PEMS_USERNAME='you@example.com'
export PEMS_PASSWORD='your-password'
